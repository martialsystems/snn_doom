# v1 closed machine (2026-09-14)

Can a discrete LIF net under a hard 8,000-unit cap run a toy Doom tick (8×8 map, 16×18 VIEW, two sprites, 3-bit ammo, 2-bit HP) with teacher-exact pose, 16 column distances, and heading hitscan?

Yes. The running stitch is 7,973 LIF units and 41,811 edges at SETTLE 29 (7,598 LIF steps per tick), leftover 27. Evidence: `checkpoints/snn_doom_v1.json`, `logs/ray_parity.json` `all_match`, `logs/tick_tape.json` `all_match`. Frozen play machine on `origin/main@890cd83`. Phase 3 museum stitch (7,217 units) is history, not this net.

## Spec that holds

Teacher `tick()` is the spec. e1 chases with `ENEMY_STEP=2`. e2 is a statue at cell (2,2). `apply_enemy2` exists and is not called from `tick()`. `TURN_STEP=2` on `N_ANG=64` (11.25° per tick). VIEW is 16 columns. Isolated RAY bake-off is still `none`; the stitch march is teacher-exact under ablation of pose vs pixels.

Host console (0 neurons): HOST_RADAR, HOST_DEAD, HostWaves for e1. Those are not LIF jobs. `--play` loads this stitch.

## Cap

`V1_NEURON_CAP` is 8,000. `doom.v1_cap` refuses a published stitch above that. Muxed walking e2 was estimated at 40 units. 27 leftover cannot buy it. Do not edit `checkpoints/snn_doom_v1.json` to fit a walker.

## Gates

| Law | Role |
|-----|------|
| `doom.v1_cap` | stitch and leftover spend ≤ 8,000 |
| `doom.settle_floor` | SETTLE not below the green held-fwd probe |
| `doom.scale_166k` | 166,700 refused until leftover units are named and RAY stays exact |
| `doom.demo_path` | host must not call `teacher.tick` / `cast_ray` / `apply_move` / `apply_enemy` / `paint_frame` |

Phase 4 in `docs/phase_log.md` is MaleCNS 166,700 sparse init. That is not a v1 bump and is not v2.

## Sequels

Walking e2 is [docs/v2.md](v2.md). 166,700 remains Phase 4.

## Revisions

- 2026-09-14: v1 closed as the 8k museum machine.
