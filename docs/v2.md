# v2 (2026-09-14)

When does a second chaser enter `tick()` without smuggling undeclared cells past `doom.v1_cap`?

Not on v1. Muxed e2 was about 40 LIF units. v1 leftover is 27. v2 is a new teacher, a new named cap, and a new stitch. It is not a v1 edit and it is not Phase 4.

## Cap (named first)

`V2_NEURON_CAP` is 12,000. Law: `doom.v2_cap`. Do not reuse `doom.v1_cap`. Do not pick 166,700 so the cap does not have to be thought through. Muxed e2 plus a little sequencer is tens of units; 12,000 is enough headroom to stitch without opening MaleCNS.

| Machine | Cap | Spec | e2 | Host | Gate |
|---------|-----|------|----|------|------|
| v1 | 8,000 | current teacher | statue | radar, waves, splash | `doom.v1_cap` |
| v2 | 12,000 | `tick()` grows | walks, same `ENEMY_STEP` | still console | `doom.v2_cap` |
| Phase 4 | 166,700 | sparse MaleCNS init | leftover already named | still console | `doom.scale_166k` |

## Teacher delta

First sentence of the v2 stitch: second chaser in `tick()`. Call `apply_enemy2` from `tick()` after `apply_enemy`. Same `ENEMY_STEP=2`. v1 tests stay pinned on the statue contract. New contract: `logs/tick_tape_v2.json`. Held-fwd may no longer require hitscan 0 and frame L1 0 if e2 can enter the heading column. `ray_parity` stays 16 integer distances.

Teacher tests green before bake-off. Same GraphForge order as v1: teacher suite, then freeze, then stitch. `checkpoints/snn_doom_v1.json` stays the museum file. `checkpoints/snn_doom_v2.json` is the later stitch.

`TURN_STEP=4` or `ENEMY_STEP=1` is a v2 teacher delta if taken, not a host poke. 32-column VIEW missed 8,000 at 8,086 on v1; it belongs in v2 only if distances stay bit-exact and readout stays WTA.

## Order

1. Write the cap (done: 12,000, `doom.v2_cap`).
2. Put `apply_enemy2` in `tick()`. Tests green.
3. Retape held-fwd and two-chaser play into `logs/tick_tape_v2.json`.
4. Stitch under 12,000. Export `checkpoints/snn_doom_v2.json` through `require_v2_cap`.
5. Optional teacher deltas (turn, enemy step, 32 columns) only after RAY distances stay exact.

## Refuse list

These stay console or Phase 4. They do not open v2:

- Host waves with more RNG
- A YOU DIED cluster in FRAME_READOUT
- SETTLE padding
- Spare latches
- Analog tau on `--play`
- `scripts/scale_166k.py`

`--play` default stays v1 until v2 tapes are green and `audit_qol` ACCEPT under `doom.v2_cap`. `--machine v2` is allowed and fail-closed if the v2 stitch or tape is missing. Do not silently load a fatter net.

Start v2 when the orange body must chase and the cap raise is accepted in the law file. Do not start it because leftover 27 feels unused. Do not start it as a path to the fly. If the next hour is spawn bias, maps, or sound, stay on the v1 host.

## Revisions

- 2026-09-17: 32-col VIEW stitch is `docs/v2_32.md` / `checkpoints/snn_doom_v2_32.json`. v2 16-col walker and checkpoint stay.
- 2026-09-16: 32-col VIEW is a sequel teacher (`docs/v2_32.md`). v2 16-col walker and checkpoint stay.
- 2026-09-16: `tick_v2` calls `apply_enemy2` after `apply_enemy`. v1 `tick()` stays the statue. Stitch `checkpoints/snn_doom_v2.json` is 9,077 LIF units and 44,420 edges under `doom.v2_cap`. Tape `logs/tick_tape_v2.json` is `all_match`. `--play` still defaults to v1.
- 2026-09-14: opened v2 spec; named cap 12,000; no stitch yet.
